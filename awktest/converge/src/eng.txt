First we open the OPC UA Configuration.
Here, for the sake of testing, we choose a local server with no security policy.
Then open kepserver
Open the properties editor to allow anonymous logins
Finally, reinitialize the runtime
Then open the browser
Search for opcua and add the dependencies
Open the documentation
Copy the demo code
Only the code that creates the session is kept here
Change it to the corresponding port
Then test if the connection is successful
You can see that the connection is successful